#ifndef __RENDER_WINDOW_h__
#define __RENDER_WINDOW_h__
#include <QWidget>

class RenderWindow :public QWidget
{
	Q_OBJECT
public:
	RenderWindow(QWidget *parent = 0, Qt::WFlags flag = 0);
	~RenderWindow(void);

private slots:
	void	resizeEvent(QResizeEvent * event);
};

#endif