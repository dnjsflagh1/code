
/******************************************************************************/

#include "stdafx.h"
#include "SPlayerSceneManager.h"
#include "SPlayer.h"

/******************************************************************************/
namespace MG
{
	//-----------------------------------------------------------------------------
	SPlayerSceneManager::SPlayerSceneManager()
	{
	}

	//-----------------------------------------------------------------------------
	SPlayerSceneManager::~SPlayerSceneManager()
	{
	}

	//------------------------------------------
	void SPlayerSceneManager::addPlayer( SPlayerPtr& playerPtr )
	{

	}

	//-----------------------------------------------------------------------------
	void SPlayerSceneManager::removePlayer( SPlayerPtr& playerPtr )
	{

	}

	//-----------------------------------------------------------------------------
	Bool SPlayerSceneManager::hasPlayer( const AccountIdType accountId )
	{
		SPlayerPtr ptr;
		if (getPlayer(ptr, accountId))
		{
			return false;
		}
		if (false == ptr.isNull())
		{
			return false;
		}

		return true;
	}

	//-----------------------------------------------------------------------------
	Bool SPlayerSceneManager::getPlayer( SPlayerPtr& ptr, const AccountIdType accountId)
	{
		return true;
	}

	//-----------------------------------------------------------------------------
	void SPlayerSceneManager::removeAllPlayer()
	{

	}

	//---------------------------------------


}	//namespace MG